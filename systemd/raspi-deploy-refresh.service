[Unit]
Description=Force redeploy of all apps (re-pulls images for patch updates)
Wants=network-online.target docker.service
After=network-online.target docker.service

[Service]
Type=oneshot
ExecStart=/bin/bash /opt/raspi/scripts/deploy.sh --force
